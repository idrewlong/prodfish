node_modules
dist
.vercel
.DS_Store
assets/source/

# AI agent skill installs (local tooling, not site source)
.agents/skills/
.claude/skills/
skills-lock.json

# asset pipeline scratch output (scripts/build-assets.mjs)
assets/.tmp-simplified/

# Playwright run output (tests/e2e is the source; these are artifacts)
test-results/
playwright-report/
blob-report/
